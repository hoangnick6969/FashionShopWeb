</div>

<!-- Athletic Streetwear Footer -->
<footer class="streetwear-footer">
    <div class="container">
        <!-- Main Footer Content -->
        <div class="footer-main">
            <!-- Brand Section -->
            <div class="footer-brand">
                <div class="brand-logo">
                    <h3>FASHION<span class="highlight">SHOP</span></h3>
                    <div class="brand-tagline">UNLEASH YOUR STYLE</div>
                </div>
                <p class="brand-description">
                    Premium athletic & streetwear for champions. Elevate your game with cutting-edge designs 
                    and superior quality that defines the streets.
                </p>
                
                <!-- Social Media -->
                <div class="social-section">
                    <h6>Connect With Us</h6>
                    <div class="social-links">
                        <a href="#" class="social-link" title="Instagram">
                            <i class="bi bi-instagram"></i>
                        </a>
                        <a href="#" class="social-link" title="TikTok">
                            <i class="bi bi-tiktok"></i>
                        </a>
                        <a href="#" class="social-link" title="YouTube">
                            <i class="bi bi-youtube"></i>
                        </a>
                        <a href="#" class="social-link" title="Twitter">
                            <i class="bi bi-twitter"></i>
                        </a>
                        <a href="#" class="social-link" title="Facebook">
                            <i class="bi bi-facebook"></i>
                        </a>
                    </div>
                </div>
            </div>
            
            <!-- Quick Links -->
            <div class="footer-section">
                <h6 class="section-title">Categories</h6>
                <ul class="footer-links">
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Athletic Wear</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Streetwear</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Sneakers</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Accessories</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Limited Edition</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Sale Items</a></li>
                </ul>
            </div>
            
            <!-- Support Section -->
            <div class="footer-section">
                <h6 class="section-title">Support</h6>
                <ul class="footer-links">
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Size Guide</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Shipping Info</a></li>
<li><a href="#"><i class="bi bi-chevron-right"></i>Returns & Exchanges</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Order Tracking</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>Contact Support</a></li>
                    <li><a href="#"><i class="bi bi-chevron-right"></i>FAQ</a></li>
                </ul>
            </div>
            
            <!-- Contact Info -->
            <div class="footer-section">
                <h6 class="section-title">Get In Touch</h6>
                <div class="contact-info">
                    <div class="contact-item">
                        <i class="bi bi-geo-alt-fill"></i>
                        <div>
                            <strong>Address:</strong><br>
                            123 Streetwear Ave<br>
                            Ho Chi Minh City, Vietnam
                        </div>
                    </div>
                    
                    <div class="contact-item">
                        <i class="bi bi-telephone-fill"></i>
                        <div>
                            <strong>Hotline:</strong><br>
                            <a href="tel:02812345678">(028) 1234 5678</a><br>
                            <a href="tel:0987654321">098 765 4321</a>
                        </div>
                    </div>
                    
                    <div class="contact-item">
                        <i class="bi bi-envelope-fill"></i>
                        <div>
                            <strong>Email:</strong><br>
                            <a href="mailto:info@fashionshop.com">info@fashionshop.com</a><br>
                            <a href="mailto:support@fashionshop.com">support@fashionshop.com</a>
                        </div>
                    </div>
                    
                    <div class="contact-item">
                        <i class="bi bi-clock-fill"></i>
                        <div>
                            <strong>Hours:</strong><br>
                            Mon - Sat: 8:00 - 22:00<br>
                            Sunday: 9:00 - 21:00
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Newsletter Section -->
        <div class="newsletter-section">
            <div class="newsletter-content">
                <div class="newsletter-text">
                    <h5>Stay In The Loop</h5>
                    <p>Get exclusive drops, early access, and 10% off your first order</p>
                </div>
                <form class="newsletter-form">
                    <div class="input-group">
                        <input type="email" placeholder="Enter your email..." required>
                        <button type="submit">
                            <i class="bi bi-send"></i>
                            Subscribe
                        </button>
                    </div>
</form>
            </div>
        </div>
        
        <!-- Footer Bottom -->
        <div class="footer-bottom">
            <div class="footer-bottom-left">
                <p>&copy; 2024 <strong>FASHIONSHOP</strong>. All rights reserved.</p>
                <p>Made with <i class="bi bi-heart-fill"></i> for streetwear culture</p>
            </div>
            <div class="footer-bottom-right">
                <a href="#">Terms of Service</a>
                <a href="#">Privacy Policy</a>
                <a href="#">Cookie Policy</a>
            </div>
        </div>
    </div>
    
    <!-- Background Elements -->
    <div class="footer-bg-element footer-bg-1"></div>
    <div class="footer-bg-element footer-bg-2"></div>
</footer>

<!-- Enhanced Footer Styles -->
<style>
/* Footer Base Styles */
.streetwear-footer {
    background: #000;
    color: #fff;
    position: relative;
    overflow: hidden;
    font-family: 'Arial', 'Helvetica Neue', sans-serif;
}

.streetwear-footer .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 2;
}

/* Background Elements */
.footer-bg-element {
    position: absolute;
    background: rgba(0, 212, 255, 0.05);
    border-radius: 50%;
    animation: float 8s ease-in-out infinite;
}

.footer-bg-1 {
    width: 300px;
    height: 300px;
    top: 20%;
    left: -5%;
    animation-delay: 0s;
}

.footer-bg-2 {
    width: 200px;
    height: 200px;
    bottom: 10%;
    right: -3%;
    animation-delay: 4s;
}

/* Main Footer Content */
.footer-main {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1.5fr;
    gap: 60px;
    padding: 80px 0 60px;
    border-bottom: 1px solid rgba(0, 212, 255, 0.2);
}

/* Brand Section */
.footer-brand {
    max-width: 400px;
}

.brand-logo h3 {
    font-size: 2.5rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 3px;
    margin-bottom: 5px;
    color: #fff;
}

.brand-logo .highlight {
    background: linear-gradient(45deg, #00d4ff, #0099cc);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.brand-tagline {
    font-size: 0.9rem;
    color: #00d4ff;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 20px;
}

.brand-description {
    color: #ccc;
    line-height: 1.6;
    margin-bottom: 30px;
    font-size: 1rem;
}

/* Social Section */
.social-section h6 {
    color: #fff;
    font-size: 1rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 15px;
}

.social-links {
    display: flex;
    gap: 15px;
}

.social-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 45px;
    height: 45px;
    background: rgba(0, 212, 255, 0.1);
    border: 2px solid rgba(0, 212, 255, 0.3);
    color: #00d4ff;
    text-decoration: none;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}
.social-link::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(0, 212, 255, 0.2), transparent);
    transition: left 0.5s ease;
}

.social-link:hover {
    background: #00d4ff;
    color: #000;
    transform: translateY(-3px);
    border-color: #00d4ff;
}

.social-link:hover::before {
    left: 100%;
}

/* Footer Sections */
.footer-section h6 {
    color: #fff;
    font-size: 1.1rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 25px;
    position: relative;
}

.footer-section h6::after {
    content: '';
    position: absolute;
    bottom: -8px;
    left: 0;
    width: 30px;
    height: 2px;
    background: linear-gradient(45deg, #00d4ff, #0099cc);
}

/* Footer Links */
.footer-links {
    list-style: none;
    padding: 0;
}

.footer-links li {
    margin-bottom: 12px;
}

.footer-links a {
    color: #ccc;
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 0.95rem;
    transition: all 0.3s ease;
    position: relative;
}

.footer-links a i {
    font-size: 0.8rem;
    color: #00d4ff;
    opacity: 0;
    transform: translateX(-10px);
    transition: all 0.3s ease;
}

.footer-links a:hover {
    color: #00d4ff;
    transform: translateX(8px);
}

.footer-links a:hover i {
    opacity: 1;
    transform: translateX(0);
}

/* Contact Info */
.contact-info {
    display: flex;
    flex-direction: column;
    gap: 20px;
}

.contact-item {
    display: flex;
    align-items: flex-start;
    gap: 12px;
}

.contact-item i {
    color: #00d4ff;
    font-size: 1.2rem;
    margin-top: 2px;
    flex-shrink: 0;
}

.contact-item div {
    flex: 1;
}

.contact-item strong {
    color: #fff;
    font-weight: 700;
    display: block;
    margin-bottom: 4px;
}

.contact-item a {
    color: #ccc;
    text-decoration: none;
    transition: color 0.3s ease;
}

.contact-item a:hover {
    color: #00d4ff;
}

/* Newsletter Section */
.newsletter-section {
    padding: 40px 0;
    background: rgba(0, 212, 255, 0.05);
    border-bottom: 1px solid rgba(0, 212, 255, 0.2);
}

.newsletter-content {
    display: grid;
    grid-template-columns: 1fr 1.5fr;
    gap: 40px;
    align-items: center;
}

.newsletter-text h5 {
    font-size: 1.8rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 8px;
    background: linear-gradient(45deg, #fff, #00d4ff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.newsletter-text p {
    color: #ccc;
    font-size: 1.1rem;
}

.newsletter-form .input-group {
    display: flex;
    background: #111;
    border: 2px solid rgba(0, 212, 255, 0.3);
    transition: border-color 0.3s ease;
}

.newsletter-form .input-group:focus-within {
    border-color: #00d4ff;
}

.newsletter-form input {
    flex: 1;
background: transparent;
    border: none;
    padding: 15px 20px;
    color: #fff;
    font-size: 1rem;
    outline: none;
}

.newsletter-form input::placeholder {
    color: #666;
}

.newsletter-form button {
    background: linear-gradient(45deg, #00d4ff, #0099cc);
    color: #000;
    border: none;
    padding: 15px 30px;
    font-weight: 700;
    text-transform: uppercase;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 8px;
}

.newsletter-form button:hover {
    background: linear-gradient(45deg, #0099cc, #00d4ff);
    transform: translateX(-2px);
}

/* Footer Bottom */
.footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 30px 0;
    flex-wrap: wrap;
    gap: 20px;
}

.footer-bottom-left p {
    margin: 0;
    color: #999;
    font-size: 0.9rem;
}

.footer-bottom-left strong {
    color: #00d4ff;
    font-weight: 700;
}

.footer-bottom-left .bi-heart-fill {
    color: #ff4757;
    animation: heartbeat 2s ease-in-out infinite;
}

.footer-bottom-right {
    display: flex;
    gap: 30px;
}

.footer-bottom-right a {
    color: #999;
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.3s ease;
    position: relative;
}

.footer-bottom-right a::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 2px;
    background: #00d4ff;
    transition: width 0.3s ease;
}

.footer-bottom-right a:hover {
    color: #00d4ff;
}

.footer-bottom-right a:hover::after {
    width: 100%;
}

/* Animations */
@keyframes float {
    0%, 100% {
        transform: translateY(0px);
    }
    50% {
        transform: translateY(-20px);
    }
}

@keyframes heartbeat {
    0%, 100% {
        transform: scale(1);
    }
    50% {
        transform: scale(1.2);
    }
}

/* Responsive Design */
@media (max-width: 1024px) {
    .footer-main {
        grid-template-columns: repeat(2, 1fr);
        gap: 40px;
    }
    
    .newsletter-content {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 30px;
    }
}

@media (max-width: 768px) {
    .footer-main {
        grid-template-columns: 1fr;
        gap: 40px;
        padding: 60px 0 40px;
    }
    
    .footer-brand {
        text-align: center;
        max-width: 100%;
    }
    
    .brand-logo h3 {
        font-size: 2rem;
    }
    
    .social-links {
        justify-content: center;
    }
    
    .footer-section {
        text-align: center;
    }
    
    .footer-section h6::after {
        left: 50%;
        transform: translateX(-50%);
    }
    
    .contact-info {
        align-items: center;
    }
    
    .contact-item {
        text-align: left;
        max-width: 300px;
    }
    
    .newsletter-form .input-group {
        flex-direction: column;
    }
    
    .newsletter-form button {
        justify-content: center;
    }
    
    .footer-bottom {
        flex-direction: column;
text-align: center;
    }
    
    .footer-bottom-right {
        flex-wrap: wrap;
        justify-content: center;
        gap: 20px;
    }
}

@media (max-width: 480px) {
    .streetwear-footer .container {
        padding: 0 15px;
    }
    
    .footer-main {
        padding: 50px 0 30px;
    }
    
    .brand-logo h3 {
        font-size: 1.8rem;
        letter-spacing: 2px;
    }
    
    .newsletter-text h5 {
        font-size: 1.5rem;
    }
    
    .footer-bottom-right {
        flex-direction: column;
        gap: 15px;
    }
}

/* Scroll Effects */
.streetwear-footer {
    animation: fadeInUp 0.8s ease-out;
}

@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Performance Optimizations */
.social-link,
.footer-links a,
.newsletter-form button {
    will-change: transform;
}

/* High Contrast Mode Support */
@media (prefers-contrast: high) {
    .streetwear-footer {
        border-top: 3px solid #00d4ff;
    }
    
    .footer-links a:hover,
    .contact-item a:hover {
        text-decoration: underline;
    }
}

/* Reduced Motion Support */
@media (prefers-reduced-motion: reduce) {
    .footer-bg-element,
    .social-link:hover,
    .footer-links a:hover,
    .bi-heart-fill {
        animation: none;
        transition: none;
    }
    
    .social-link:hover,
    .footer-links a:hover {
        transform: none;
    }
}
</style>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
// Enhanced Footer Interactions
document.addEventListener('DOMContentLoaded', function() {
    // Newsletter form handling
    const newsletterForm = document.querySelector('.newsletter-form');
    if (newsletterForm) {
        newsletterForm.addEventListener('submit', function(e) {
            e.preventDefault();
            const email = this.querySelector('input[type="email"]').value;
            
            if (email) {
                // Show success feedback
                const button = this.querySelector('button');
                const originalText = button.innerHTML;
                
                button.innerHTML = '<i class="bi bi-check-circle"></i> Subscribed!';
                button.style.background = 'linear-gradient(45deg, #10b981, #059669)';
                
                setTimeout(() => {
                    button.innerHTML = originalText;
                    button.style.background = 'linear-gradient(45deg, #00d4ff, #0099cc)';
                    this.querySelector('input[type="email"]').value = '';
                }, 3000);
            }
        });
    }

    // Smooth scroll for footer links
    document.querySelectorAll('.footer-links a[href^="#"]').forEach(link => {
        link.addEventListener('click', function(e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
target.scrollIntoView({ behavior: 'smooth' });
            }
        });
    });

    // Add hover effects for contact items
    document.querySelectorAll('.contact-item').forEach(item => {
        item.addEventListener('mouseenter', function() {
            this.style.transform = 'translateX(5px)';
            this.style.transition = 'transform 0.3s ease';
        });
        
        item.addEventListener('mouseleave', function() {
            this.style.transform = 'translateX(0)';
        });
    });

    // Social link analytics tracking
    document.querySelectorAll('.social-link').forEach(link => {
        link.addEventListener('click', function(e) {
            const platform = this.title.toLowerCase();
            console.log(`Social link clicked: ${platform}`);
            // Add your analytics tracking here
        });
    });

    // Email validation for newsletter
    const emailInput = document.querySelector('.newsletter-form input[type="email"]');
    if (emailInput) {
        emailInput.addEventListener('blur', function() {
            const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (this.value && !emailPattern.test(this.value)) {
                this.style.borderColor = '#ff4757';
            } else {
                this.style.borderColor = 'transparent';
            }
        });
    }

    // Add intersection observer for footer animations
    if ('IntersectionObserver' in window) {
        const footerObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.animationPlayState = 'running';
                }
            });
        }, { threshold: 0.1 });

        document.querySelectorAll('.footer-section').forEach(section => {
            footerObserver.observe(section);
        });
    }
});

// Add ripple effect to buttons
function addRippleEffect(element) {
    element.addEventListener('click', function(e) {
        const ripple = document.createElement('span');
        const rect = this.getBoundingClientRect();
        const size = Math.max(rect.width, rect.height);
        const x = e.clientX - rect.left - size / 2;
        const y = e.clientY - rect.top - size / 2;
        
        ripple.style.cssText = `
            position: absolute;
            width: ${size}px;
            height: ${size}px;
            left: ${x}px;
            top: ${y}px;
            background: rgba(255,255,255,0.4);
            border-radius: 50%;
            transform: scale(0);
            animation: ripple 0.6s linear;
            pointer-events: none;
            z-index: 1;
        `;
        
        this.style.position = 'relative';
        this.style.overflow = 'hidden';
        this.appendChild(ripple);
        
        setTimeout(() => ripple.remove(), 600);
    });
}

// Apply ripple effect to interactive elements
document.querySelectorAll('.newsletter-form button, .social-link').forEach(addRippleEffect);

// Add CSS for ripple animation
const rippleStyle = document.createElement('style');
rippleStyle.textContent = `
    @keyframes ripple {
        to {
            transform: scale(4);
            opacity: 0;
        }
    }
`;
document.head.appendChild(rippleStyle);
</script>
</body>
</html>